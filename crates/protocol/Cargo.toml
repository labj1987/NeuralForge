[package]
name = "neuralforge-protocol"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Shared-memory wire format between the neuralforge layer, helper, GUI and CLI"

[lib]
name = "neuralforge_protocol"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
